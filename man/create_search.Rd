% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searches.R
\name{create_search}
\alias{create_search}
\title{Create a new saved search in Zotero library}
\usage{
create_search(client, name, conditions)
}
\arguments{
\item{client}{Zotero API client object}

\item{name}{Name of the saved search}

\item{conditions}{List of search conditions. Each condition should be a list with
'condition', 'operator', and 'value' elements}
}
\value{
Response from the API containing the new saved search details
}
\description{
Create a new saved search in Zotero library
}
\examples{
\dontrun{
client <- zotero_client(
  library_id = "123456",
  library_type = "user",
  api_key = "your_api_key"
)

# Create a new saved search
conditions <- list(
  list(
    condition = "title",
    operator = "contains",
    value = "machine learning"
  ),
  list(
    condition = "itemType",
    operator = "is",
    value = "journalArticle"
  )
)

new_search <- create_search(
  client, 
  name = "ML Articles",
  conditions = conditions
)
}
}
